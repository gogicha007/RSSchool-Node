import { IPlayer, IRoom, IWinner } from './interfaces';

export const PlayerModel = [] as IPlayer[];
export const RoomsModel = [] as IRoom[];
export const WinnerModel = [] as IWinner[];
export const GamesModel = [] as string[];

export const Sessions = new Set();
